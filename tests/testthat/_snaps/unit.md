# deprecated connect arguments warn and are stripped

    The `compression` argument of `dbConnect()` is deprecated as of RClickhouse 2.0.0.
    i Arrow transports negotiate compression automatically; the argument is ignored.

